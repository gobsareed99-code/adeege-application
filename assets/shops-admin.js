import{createClient}from'https://esm.sh/@supabase/supabase-js@2.57.4';
const sb=createClient('https://tjwoqvqfavxqykvbasaf.supabase.co','sb_publishable_3XeoEp9EqRqPLPIm5Cwthg_Pf1lEYUZ');
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
let stores=[],editing=null,session=null;
function msg(text,err=false){const x=$('#formMsg');x.textContent=text;x.className='msg show'+(err?' err':'')}
async function requireAdmin(){const{data:{session:s}}=await sb.auth.getSession();session=s;if(!s){document.body.innerHTML='<div style="font-family:Arial;padding:40px"><h2>Admin login required</h2><p>Please log in to ADEEGE first, then return to this page.</p><a href="app.html">Open ADEEGE</a></div>';return false}const{data,error}=await sb.rpc('adeege_is_admin');if(error||!data){document.body.innerHTML='<div style="font-family:Arial;padding:40px"><h2>Admin access required</h2><p>This page is available only to the ADEEGE administrator.</p><a href="app.html">Return to ADEEGE</a></div>';return false}return true}
async function loadOwners(){const{data}=await sb.from('marketplace_profiles').select('id,full_name,phone,role').in('role',['seller','admin']).order('full_name');$('#shopOwner').innerHTML='<option value="">ADEEGE managed / unassigned</option>'+(data||[]).map(x=>`<option value="${x.id}">${esc(x.full_name||'Seller')} · ${esc(x.phone||x.role)}</option>`).join('')}
async function loadStores(){const{data,error}=await sb.from('marketplace_stores').select('*').order('created_at',{ascending:false});if(error){$('#shopsList').innerHTML=`<div class="empty">${esc(error.message)}</div>`;return}stores=data||[];renderStores()}
function renderStores(){const q=$('#shopSearch').value.trim().toLowerCase();const rows=stores.filter(s=>!q||[s.name,s.category,s.location,s.phone,s.whatsapp,s.email].some(v=>String(v||'').toLowerCase().includes(q)));$('#shopCount').textContent=`${rows.length} shop${rows.length===1?'':'s'}`;$('#shopsList').innerHTML=rows.length?rows.map(s=>`<article class="shop"><img src="${esc(s.logo_url||'assets/adeege-logo.jpg')}" alt=""><div><h3>${esc(s.name)} ${s.verified?'✓':''}</h3><p>📍 ${esc(s.location||'Mogadishu')} ${s.category?'· '+esc(s.category):''}</p><p>${s.phone?'📞 '+esc(s.phone):''} ${s.whatsapp?' · WhatsApp '+esc(s.whatsapp):''}</p><span class="badge ${s.status==='approved'?'':'off'}">${esc(s.status)}</span></div><div class="shop-actions"><button class="mini" data-edit="${s.id}">Edit</button><button class="mini" data-toggle="${s.id}" data-next="${s.status==='approved'?'rejected':'approved'}">${s.status==='approved'?'Suspend':'Activate'}</button></div></article>`).join(''):'<div class="empty">No shops found.</div>';$$('[data-edit]').forEach(b=>b.onclick=()=>editStore(b.dataset.edit));$$('[data-toggle]').forEach(b=>b.onclick=async()=>{const{error}=await sb.from('marketplace_stores').update({status:b.dataset.next,updated_at:new Date().toISOString()}).eq('id',b.dataset.toggle);if(error)return alert(error.message);await loadStores()})}
function resetForm(){editing=null;$('#shopForm').reset();$('#shopVerified').checked=false;$('#logoPreview').classList.remove('show');$('#formTitle').textContent='Add New Market Shop';$('#saveShopBtn').textContent='Create Shop';$('#cancelEdit').style.display='none';$('#formMsg').className='msg'}
function editStore(id){const s=stores.find(x=>x.id===id);if(!s)return;editing=s;$('#formTitle').textContent='Edit Market Shop';$('#saveShopBtn').textContent='Save Changes';$('#cancelEdit').style.display='inline-block';$('#shopOwner').value=s.owner_id||'';$('#shopName').value=s.name||'';$('#shopCategory').value=s.category||'';$('#shopPhone').value=s.phone||'';$('#shopWhatsapp').value=s.whatsapp||'';$('#shopEmail').value=s.email||'';$('#shopLocation').value=s.location||'';$('#shopAddress').value=s.address||'';$('#shopHours').value=s.business_hours||'';$('#shopFacebook').value=s.facebook_url||'';$('#shopTiktok').value=s.tiktok_url||'';$('#shopDescription').value=s.description||'';$('#shopVerified').checked=!!s.verified;if(s.logo_url){$('#logoPreview').src=s.logo_url;$('#logoPreview').classList.add('show')}window.scrollTo({top:0,behavior:'smooth'})}
async function uploadLogo(){const f=$('#shopLogo').files?.[0];if(!f)return editing?.logo_url||'';if(!f.type.startsWith('image/'))throw new Error('Please choose an image file.');if(f.size>5*1024*1024)throw new Error('Logo must be 5 MB or smaller.');const clean=(f.name||'logo.jpg').replace(/[^a-zA-Z0-9._-]/g,'-');const path=`admin/${Date.now()}-${clean}`;const up=await sb.storage.from('store-logos').upload(path,f,{contentType:f.type,cacheControl:'3600'});if(up.error)throw up.error;return sb.storage.from('store-logos').getPublicUrl(path).data.publicUrl}
$('#shopLogo').onchange=()=>{const f=$('#shopLogo').files?.[0];if(!f)return;$('#logoPreview').src=URL.createObjectURL(f);$('#logoPreview').classList.add('show')};
$('#shopSearch').oninput=renderStores;$('#cancelEdit').onclick=resetForm;
$('#shopForm').onsubmit=async e=>{e.preventDefault();const btn=$('#saveShopBtn');btn.disabled=true;btn.textContent='Saving...';try{const logo=await uploadLogo();const payload={owner_id:$('#shopOwner').value||null,name:$('#shopName').value.trim(),category:$('#shopCategory').value,phone:$('#shopPhone').value.trim()||null,whatsapp:$('#shopWhatsapp').value.trim()||null,email:$('#shopEmail').value.trim()||null,location:$('#shopLocation').value.trim()||'Mogadishu',address:$('#shopAddress').value.trim()||null,business_hours:$('#shopHours').value.trim()||null,facebook_url:$('#shopFacebook').value.trim()||null,tiktok_url:$('#shopTiktok').value.trim()||null,description:$('#shopDescription').value.trim()||null,logo_url:logo||null,verified:$('#shopVerified').checked};if(!payload.name)throw new Error('Shop name is required.');if(editing){const{error}=await sb.from('marketplace_stores').update({...payload,updated_at:new Date().toISOString()}).eq('id',editing.id);if(error)throw error;msg('Shop details updated successfully.');}else{const{error}=await sb.rpc('marketplace_admin_create_store',{p_owner_id:payload.owner_id,p_name:payload.name,p_category:payload.category,p_phone:payload.phone,p_whatsapp:payload.whatsapp,p_email:payload.email,p_location:payload.location,p_address:payload.address,p_description:payload.description,p_business_hours:payload.business_hours,p_logo_url:payload.logo_url,p_facebook_url:payload.facebook_url,p_tiktok_url:payload.tiktok_url,p_verified:payload.verified});if(error)throw error;msg('Market shop created and published successfully.');}await loadStores();setTimeout(resetForm,1200)}catch(err){msg(err.message||String(err),true)}finally{btn.disabled=false;btn.textContent=editing?'Save Changes':'Create Shop'}};
if(await requireAdmin()){await Promise.all([loadOwners(),loadStores()])}