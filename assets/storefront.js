(()=>{'use strict';const URL='https://tjwoqvqfavxqykvbasaf.supabase.co',KEY='sb_publishable_3XeoEp9EqRqPLPIm5Cwthg_Pf1lEYUZ';const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const money=n=>'$'+Number(n||0).toLocaleString(undefined,{maximumFractionDigits:2});let store=null,products=[];function toast(t){const x=$('#toast');x.textContent=t;x.classList.add('show');clearTimeout(window.__st);window.__st=setTimeout(()=>x.classList.remove('show'),2200)}function cart(){try{return JSON.parse(localStorage.getItem('adeege_v2_cart')||'[]')}catch{return[]}}function saveCart(c){localStorage.setItem('adeege_v2_cart',JSON.stringify(c));renderCartCount()}function renderCartCount(){const n=cart().reduce((a,b)=>a+Number(b.quantity||0),0);$('#cartCount').textContent=n}async function api(path){const c=new AbortController(),tm=setTimeout(()=>c.abort(),10000);try{const r=await fetch(URL+'/rest/v1/'+path,{headers:{apikey:KEY,Authorization:'Bearer '+KEY},signal:c.signal});if(!r.ok)throw new Error('Could not load shop.');return await r.json()}finally{clearTimeout(tm)}}function renderStore(){if(!store)return;document.title=store.name+' — ADEEGE';const phone=store.phone||store.whatsapp||'',wa=(store.whatsapp||store.phone||'').replace(/[^0-9]/g,'');$('#storeHero').innerHTML=`<div class="store-info"><img class="store-logo" src="${esc(store.logo_url||'assets/adeege-logo.jpg')}" alt="${esc(store.name)}"><div><h1>${esc(store.name)} ${store.verified?'<span class="verified">✓ Verified</span>':''}</h1><div class="store-meta"><span class="chip">📍 ${esc(store.location||'Mogadishu')}</span>${store.category?`<span class="chip">🏷 ${esc(store.category)}</span>`:''}${store.business_hours?`<span class="chip">🕒 ${esc(store.business_hours)}</span>`:''}</div><p class="store-desc">${esc(store.description||'Local seller available through ADEEGE marketplace.')}</p><div class="store-actions">${phone?`<a href="tel:${esc(phone)}">📞 Call Shop</a>`:''}${wa?`<a href="https://wa.me/${wa}" target="_blank" rel="noopener">💬 WhatsApp</a>`:''}${store.facebook_url?`<a href="${esc(store.facebook_url)}" target="_blank" rel="noopener">Facebook</a>`:''}${store.tiktok_url?`<a href="${esc(store.tiktok_url)}" target="_blank" rel="noopener">TikTok</a>`:''}</div></div></div>`}function renderProducts(list=products){$('#productCount').textContent=`${list.length} product${list.length===1?'':'s'} available`;$('#productGrid').innerHTML=list.length?list.map(p=>`<article class="product"><div class="product-img">${p.image_url?`<img src="${esc(p.image_url)}" alt="${esc(p.name)}" loading="lazy">`:'🛍️'}<span class="stock">${Number(p.quantity)>0?Number(p.quantity)+' in stock':'Out of stock'}</span></div><div class="product-body"><h3>${esc(p.name)}</h3><p class="desc">${esc((p.description||'').slice(0,130))}</p><div class="price">${money(p.price)}</div><div class="actions"><button class="add" data-add="${p.id}" ${Number(p.quantity)<=0?'disabled':''}>🛒 Add to Cart</button><a class="view" href="app.html?product=${encodeURIComponent(p.id)}#products">Marketplace</a></div></div></article>`).join(''):'<div class="empty">This shop has no published products yet.</div>';document.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>{const p=products.find(x=>x.id===b.dataset.add);if(!p||Number(p.quantity)<=0)return;const c=cart(),old=c.find(x=>x.product_id===p.id);if(old)old.quantity=Math.min(Number(p.quantity),Number(old.quantity)+1);else c.push({product_id:p.id,quantity:1});saveCart(c);toast('Added to cart')})}async function init(){renderCartCount();const id=new URLSearchParams(location.search).get('store');if(!id){$('#storeHero').innerHTML='<div class="empty">Shop not found.</div>';$('#productGrid').innerHTML='';return}try{const s=await api(`marketplace_stores?id=eq.${encodeURIComponent(id)}&status=eq.approved&select=*`);store=s[0];if(!store)throw new Error('This shop is unavailable.');renderStore();products=await api(`marketplace_products?store_id=eq.${encodeURIComponent(id)}&status=eq.approved&select=id,name,description,category,price,quantity,image_url&order=created_at.desc`);$('#productTitle').textContent=store.name+' Products';renderProducts()}catch(e){$('#storeHero').innerHTML=`<div class="empty">${esc(e.message||String(e))}</div>`;$('#productGrid').innerHTML=''}}$('#productSearch').addEventListener('input',e=>{const q=e.target.value.trim().toLowerCase();renderProducts(products.filter(p=>!q||String(p.name).toLowerCase().includes(q)||String(p.description||'').toLowerCase().includes(q)))})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();